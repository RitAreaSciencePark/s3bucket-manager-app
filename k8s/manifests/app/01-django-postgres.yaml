# PostgreSQL for Django application data.
# Values must match backend-config/backend-secret database settings.
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: django-postgres-pvc
  namespace: bucket-explorer
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: django-postgres
  namespace: bucket-explorer
  labels:
    app: django-postgres
    component: database
spec:
  replicas: 1
  selector:
    matchLabels:
      app: django-postgres
  template:
    metadata:
      labels:
        app: django-postgres
        component: database
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          env:
            - name: POSTGRES_DB
              value: "djangodb"
            - name: POSTGRES_USER
              value: "djangouser"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: backend-secret
                  key: database-password
          ports:
            - containerPort: 5432
              name: postgres
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
          # PostgreSQL stores Django metadata only (not object data). IO-bound, not CPU-bound.
          # With gunicorn 2 workers + admin panel, expect ~10-15 concurrent DB connections.
          # Prod steady-state observed ~3m CPU / ~27Mi RAM; 512Mi limit leaves ample headroom
          # for metadata growth while freeing namespace quota (limits.cpu / limits.memory).
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - djangouser
                - -d
                - djangodb
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 6
          livenessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - djangouser
                - -d
                - djangodb
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 6
      volumes:
        - name: postgres-data
          persistentVolumeClaim:
            claimName: django-postgres-pvc

---
apiVersion: v1
kind: Service
metadata:
  name: django-postgres
  namespace: bucket-explorer
spec:
  type: ClusterIP
  selector:
    app: django-postgres
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
