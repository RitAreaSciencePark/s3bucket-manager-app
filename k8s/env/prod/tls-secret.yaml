# Template only. Never commit the real certificate or private key.
# Production operators must use a gitignored tls-secret.local.{yaml,json} file.
apiVersion: v1
kind: Secret
metadata:
  name: wildcard.areasciencepark.it-certs
  namespace: bucket-explorer
type: kubernetes.io/tls
stringData:
  tls.crt: |
    REPLACE_WITH_PRODUCTION_CERTIFICATE_PEM
  tls.key: |
    REPLACE_WITH_PRODUCTION_PRIVATE_KEY_PEM
